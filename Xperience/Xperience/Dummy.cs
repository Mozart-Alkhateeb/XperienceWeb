using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace Xperience
{
    public class Dummy
    {
        /*
I'll create a user table + sites and the relation between them, then you can modify things to suite your needs
There is no benefit of prefixing a column with the table name 
or adding an x to the table name
N.P stant for navigation propoerty
F.K stands for Foreign Key

Steps to applying model changes to db
go the project folder (cd .\Xperience.Data)
Add a migration with a unique name (dotnet ef migrations add InitalCreate)
dotnet ef database update
         */
    }
}
